diff --git a/app/src/main/java/com/kingzcheung/xime/rime/RimeEngine.kt b/app/src/main/java/com/kingzcheung/xime/rime/RimeEngine.kt
index 88e78c8..5adbd5d 100644
--- a/app/src/main/java/com/kingzcheung/xime/rime/RimeEngine.kt
+++ b/app/src/main/java/com/kingzcheung/xime/rime/RimeEngine.kt
@@ -283,6 +283,32 @@ class RimeEngine {
         }
     }
 
+    /**
+     * key-process 线程用：阻塞拿锁。
+     * [processKeyAndGetResult] 走 tryLocked，主线程 UI 刷新占锁时会 processed=false，
+     * 单码后紧跟标点会被当成空闲直上屏。
+     */
+    fun processKeyAndGetResultBlocking(keycode: Int, mask: Int): RimeProcessResult {
+        if (!isInitialized) return RimeProcessResult(false, "", "", "", emptyArray(), false, false, false)
+        return locked {
+            if (!nativeHasSession() && !nativeCreateSession())
+                return@locked RimeProcessResult(false, "", "", "", emptyArray(), false, false, false)
+            nativeProcessKeyAndGetResult(keycode, mask)
+        }
+    }
+
+    /**
+     * key-process 线程用：阻塞读当前码。
+     * [getInput] 走 tryLocked，拿不到锁返回空串，会把有码判成空闲。
+     */
+    fun getInputBlocking(): String {
+        if (!isInitialized) return ""
+        return locked {
+            if (!nativeHasSession()) return@locked ""
+            nativeGetInput() ?: ""
+        }
+    }
+
     fun getProcessResult(processed: Boolean): RimeProcessResult {
         if (!isInitialized) return RimeProcessResult(false, "", "", "", emptyArray(), false, false, false)
         // 必须持 rimeLock：nativeGetProcessResult 内部 RimeGetContext → Menu::Prepare
@@ -429,12 +455,16 @@ class RimeEngine {
 
     fun setOption(option: String, value: Boolean) {
         if (!nativeHasSession()) return
-        nativeSetOption(option, value)
+        tryLocked(Unit) {
+            nativeSetOption(option, value)
+        }
     }
 
     fun getOption(option: String): Boolean {
         if (!nativeHasSession()) return false
-        return nativeGetOption(option)
+        return tryLocked(false) {
+            nativeGetOption(option)
+        }
     }
 
     fun setPageSize(schemaId: String, pageSize: Int) {
